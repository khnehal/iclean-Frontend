export const initialState = {
  successMessage: '',
  errorsList: [],
};

export const successMessage = (state = initialState) => state.successMessage;
export const errorsList = (state = initialState) => state.errorsList;
